name: Deploy React (src-react) to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4

      - name: Configurazione Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # Specifica dove si trova il package-lock.json per la cache
          cache-dependency-path: 'src-react/package-lock.json'
          cache: 'npm'

      - name: Installazione dipendenze
        working-directory: ./src-react
        run: npm ci

      - name: Build del progetto React
        working-directory: ./src-react
        run: npm run build

      - name: Configura GitHub Pages
        uses: actions/configure-pages@v5

      - name: Carica l'artifact dalla cartella www
        uses: actions/upload-pages-artifact@v3
        with:
          # Se la build genera la cartella 'www' nella radice del progetto:
          path: './www'

      - name: Deploy su GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
